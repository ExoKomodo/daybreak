importc "string.h"

fun do_work (a: int) int is
	( printf
		"a: %d, 'Hello world' length: %llu\n"
		a
		(strlen "Hello world") )
	
	match
	  (equals a 0) => return 0
		(default) => return (do_work (sub a 1))
	end
end

#include <netdb.h>
#include <sys/socket.h>
#include <sys/types.h>

char* get_ipv4_from_host(const char*);
char* get_ipv4_from_host(const char* host) {
	// Reference https://stackoverflow.com/q/52727565
	return NULL;
}

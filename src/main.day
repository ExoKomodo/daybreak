importc "api/prelude.h"

fun main (argc:int argv:ptr<ccstring>) int is
	(daybreak_greeting)
	
	let parsed: DaybreakArgParseResult <- (daybreak_parse_args argc argv)
	match
		(not parsed.is_success) => return 1
	end

	return (daybreak_compile parsed.args)
end
